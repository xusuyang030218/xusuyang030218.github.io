---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="关于" description="关于徐苏洋：软件工程、LLM 应用、Java 全栈、企业前端重构与 Agent Skills 实践。">
  <header class="about-hero container">
    <div>
      <p class="kicker">About</p>
      <h1>软件工程学生，<br />也是证据驱动的开发者。</h1>
    </div>
    <div class="about-intro">
      <img src="/avatar.png" width="640" height="640" alt="徐苏洋的公开头像" />
      <p>我关注大模型应用如何进入真实系统，也关心遗留代码、团队协作、测试和部署这些不够耀眼却决定交付质量的部分。</p>
    </div>
  </header>

  <section class="section container profile-grid reveal">
    <div class="profile-title">
      <p class="kicker">Profile</p>
      <h2>徐苏洋</h2>
      <p class="mono">2027 届 · 软件工程</p>
    </div>
    <div class="profile-copy">
      <p>就读于大连东软信息学院软件工程专业。实践方向覆盖 LLM 应用、RAG、Agent、Java 后端、前端可视化与工程化部署。</p>
      <p>在亚信科技担任前端开发实习生期间，参与 DTS Dashboard 版本升级开发与前端重构，以及数据资产计量计费平台的 React 开发、联调与测试。</p>
      <div class="contact-links">
        <a class="button" href="mailto:2307456010@qq.com">发送邮件</a>
        <a class="button secondary" href="https://github.com/xusuyang030218">GitHub</a>
        <a class="button secondary" href="https://gitee.com/anhuiwuhu_xsy">Gitee</a>
      </div>
    </div>
  </section>

  <section class="section skills-section">
    <div class="container">
      <p class="kicker">Capability Map</p>
      <div class="skill-columns">
        <article>
          <h2>01</h2>
          <h3>大模型应用</h3>
          <p>RAG、LangChain、向量检索、重排、Prompt 工程、SSE、Agent 执行与评估。</p>
        </article>
        <article>
          <h2>02</h2>
          <h3>后端工程</h3>
          <p>Java、Spring Boot、MyBatis Plus、MySQL、Redis、Redisson、Kafka 与 Docker Compose。</p>
        </article>
        <article>
          <h2>03</h2>
          <h3>前端与重构</h3>
          <p>Vue 2/3、React、Element UI、Ant Design、ECharts，以及遗留系统问题定位与渐进式重构。</p>
        </article>
        <article>
          <h2>04</h2>
          <h3>Agent Skills</h3>
          <p>将重构、代码检视、文档摄取、测试与协作方法沉淀为可复用执行协议。</p>
        </article>
      </div>
    </div>
  </section>

  <section class="section container timeline reveal">
    <div>
      <p class="kicker">Experience</p>
      <h2>经历</h2>
    </div>
    <ol>
      <li>
        <time>2026.06 至今</time>
        <div><h3>亚信科技 · 前端开发实习生</h3><p>DTS Dashboard 版本升级与重构；计量计价平台 React 开发、联调与测试。</p></div>
      </li>
      <li>
        <time>2023 - 2027</time>
        <div><h3>大连东软信息学院 · 软件工程</h3><p>班级排名 1/33，曾任极限编程社团社长与学习委员。</p></div>
      </li>
    </ol>
  </section>

  <section class="section honors-section">
    <div class="container honors-grid">
      <div>
        <p class="kicker">Selected Honors</p>
        <h2>把竞赛能力转化为工程判断。</h2>
        <p class="lede">学校系统记录学科竞赛获奖 35 项次，其中国际级 1 项、国家级 13 项、省级 21 项。</p>
      </div>
      <div class="honor-list">
        <p><b>2025</b><span>美国大学生数学建模竞赛 Honorable Mention</span></p>
        <p><b>2024 / 2025</b><span>蓝桥杯辽宁赛区 C/C++ 一等奖</span></p>
        <p><b>2024 / 2025</b><span>蓝桥杯全国总决赛 C/C++ 三等奖</span></p>
        <p><b>2025</b><span>全球校园人工智能算法精英大赛二等奖</span></p>
        <p><b>2025</b><span>中软国际卓越杯大数据挑战赛一等奖</span></p>
      </div>
    </div>
  </section>

  <section class="section container principles reveal">
    <p class="kicker">Principles</p>
    <div>
      <blockquote>不把调研写成实现。</blockquote>
      <blockquote>不把仓库总量写成个人贡献。</blockquote>
      <blockquote>不让 AI 替人承担最终判断。</blockquote>
    </div>
  </section>
</BaseLayout>

<style>
  .about-hero {
    display: grid;
    min-height: calc(100dvh - 74px);
    grid-template-columns: minmax(0, 1.35fr) minmax(17rem, 0.65fr);
    gap: clamp(3rem, 8vw, 8rem);
    align-items: center;
    padding-block: clamp(4rem, 10vw, 8rem);
  }

  .about-hero h1 {
    max-width: 13ch;
    font-size: clamp(3rem, 7vw, 7rem);
  }

  .about-intro {
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow);
    padding: 1.5rem;
  }

  .about-intro img {
    width: min(100%, 16rem);
    aspect-ratio: 1;
    object-fit: cover;
    margin-bottom: 1.5rem;
    border: var(--border);
    background: var(--cyan);
    filter: grayscale(1) contrast(1.08);
  }

  .about-intro p {
    color: var(--ink-soft);
    font-size: 1.08rem;
    font-weight: 600;
  }

  .profile-grid,
  .timeline,
  .honors-grid {
    display: grid;
    grid-template-columns: 0.75fr 1.25fr;
    gap: clamp(3rem, 8vw, 8rem);
  }

  .profile-title h2 {
    margin-bottom: 0.7rem;
    width: fit-content;
    border: var(--border);
    background: var(--yellow);
    box-shadow: var(--shadow-sm);
    padding: 0 0.6rem;
  }

  .profile-copy {
    color: var(--ink-soft);
    font-size: 1.08rem;
    font-weight: 600;
  }

  .contact-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
    margin-top: 2rem;
  }

  .skills-section {
    border-block: var(--border);
    background: var(--purple);
  }

  .skill-columns {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
  }

  .skill-columns article {
    min-height: 22rem;
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow);
    padding: 1.2rem;
  }

  .skill-columns article:nth-child(2) {
    background: var(--yellow);
  }

  .skill-columns article:nth-child(3) {
    background: var(--green);
  }

  .skill-columns h2 {
    font: 800 0.78rem var(--mono);
  }

  .skill-columns h3 {
    margin-top: 7rem;
  }

  .skill-columns p {
    color: var(--ink-soft);
    font-size: 0.9rem;
    font-weight: 600;
  }

  .timeline ol {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .timeline li {
    display: grid;
    grid-template-columns: 8rem 1fr;
    gap: 1.5rem;
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow-sm);
    margin-bottom: 1rem;
    padding: 1.4rem;
  }

  .timeline time {
    font: 700 0.72rem var(--mono);
  }

  .timeline h3 {
    margin-bottom: 0.6rem;
  }

  .timeline p {
    margin: 0;
    color: var(--ink-soft);
    font-weight: 600;
  }

  .honors-section {
    border-block: var(--border);
    background: var(--yellow);
  }

  .honor-list p {
    display: grid;
    grid-template-columns: 7rem 1fr;
    gap: 1rem;
    margin: 0 0 0.75rem;
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow-sm);
    padding: 1rem;
  }

  .honor-list b {
    font: 800 0.72rem var(--mono);
  }

  .principles div {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .principles blockquote {
    margin: 0;
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow);
    padding: 1.2rem;
    color: var(--ink);
    font-size: clamp(1.3rem, 2.5vw, 2rem);
    font-weight: 900;
    line-height: 1.15;
  }

  .principles blockquote:nth-child(2) {
    background: var(--cyan);
  }

  .principles blockquote:nth-child(3) {
    background: var(--pink);
  }

  @media (max-width: 800px) {
    .about-hero,
    .profile-grid,
    .timeline,
    .honors-grid {
      min-height: auto;
      grid-template-columns: 1fr;
    }

    .skill-columns,
    .principles div {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 520px) {
    .skill-columns,
    .principles div {
      grid-template-columns: 1fr;
    }

    .skill-columns article {
      min-height: auto;
    }

    .skill-columns h3 {
      margin-top: 3rem;
    }

    .timeline li,
    .honor-list p {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
  }
</style>
