---
import type { ProjectEvidence } from '../data/evidence';

interface Props {
  evidence: ProjectEvidence;
  projectTitle: string;
}

const { evidence, projectTitle } = Astro.props;
---

<section class="evidence-dossier container" aria-labelledby="evidence-title">
  <header class="dossier-header">
    <div>
      <p class="kicker">Evidence Dossier</p>
      <h2 id="evidence-title">证据卷宗</h2>
    </div>
    <div class="verification-stamp">
      <span class="mono">VERIFICATION</span>
      <strong>{evidence.verification}</strong>
    </div>
  </header>

  <p class="verification-note">{evidence.verificationNote}</p>

  {evidence.screenshots && evidence.screenshots.length > 0 && (
    <div class="screenshot-gallery reveal" aria-label="项目截图证据">
      {evidence.screenshots.map((shot) => (
        <figure class:list={['shot', shot.span ? 'shot-span' : '']}>
          <img src={shot.src} alt={shot.alt} loading="lazy" decoding="async" width="1600" height="900" />
          <figcaption>{shot.caption}</figcaption>
        </figure>
      ))}
    </div>
  )}

  {evidence.media && (
    <figure class="evidence-media reveal">
      {evidence.media.videoWebm ? (
        <video controls playsinline preload="metadata" poster={evidence.media.poster} aria-label={`${projectTitle} 演示视频`}>
          {evidence.media.videoMp4 && <source src={evidence.media.videoMp4} type="video/mp4" />}
          <source src={evidence.media.videoWebm} type="video/webm" />
          当前浏览器不能播放视频，请阅读下方演示文字说明。
        </video>
      ) : (
        <img src={evidence.media.poster} width="1440" height="900" loading="lazy" alt={evidence.media.caption} />
      )}
      <figcaption>
        <strong>{evidence.media.caption}</strong>
        <details>
          <summary>查看视频文字说明</summary>
          <ol>{evidence.media.transcript.map((line) => <li>{line}</li>)}</ol>
        </details>
      </figcaption>
    </figure>
  )}

  {evidence.links && evidence.links.length > 0 && (
    <div class="source-links reveal">
      <h3>可点击公开证据</h3>
      <div class="source-grid">
        {evidence.links.map((link) => (
          <a href={link.url} rel="noopener">
            <span class="mono">{link.kind.toUpperCase()}</span>
            <strong>{link.label}</strong>
            <small>{link.note}</small>
            <b aria-hidden="true">↗</b>
          </a>
        ))}
      </div>
    </div>
  )}

  {evidence.commits.length > 0 && (
    <div class="commit-ledger reveal">
      <h3>提交账本</h3>
      <div class="commit-table" role="table" aria-label={`${projectTitle} 提交证据`}>
        {evidence.commits.map((commit) => (
          <div class="commit-row" role="row">
            <time role="cell">{commit.date}</time>
            <code role="cell">{commit.hash}</code>
            <div role="cell"><strong>{commit.subject}</strong>{commit.stat && <small>{commit.stat}</small>}</div>
            {commit.url ? <a href={commit.url} rel="noopener" aria-label={`打开提交 ${commit.hash}`}>打开 ↗</a> : <span class="private-mark mono">INTERNAL</span>}
          </div>
        ))}
      </div>
    </div>
  )}

  {evidence.code && evidence.code.length > 0 && (
    <div class="code-evidence">
      {evidence.code.map((item) => (
        <article class="code-case reveal">
          <header>
            <div>
              <span class="mono">{item.language}</span>
              <h3>{item.title}</h3>
            </div>
            {item.sourceUrl && <a href={item.sourceUrl} rel="noopener">查看固定源码 ↗</a>}
          </header>
          <div class="file-bar"><code>{item.file}</code><span>{item.lines}</span></div>
          <pre tabindex="0"><code>{item.code}</code></pre>
          <p>{item.note}</p>
        </article>
      ))}
    </div>
  )}
</section>

<style>
  .evidence-dossier {
    padding-block: var(--section);
    border-top: 1px solid var(--ink);
  }

  .dossier-header {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .dossier-header h2 {
    margin-bottom: 0;
  }

  .verification-stamp {
    display: grid;
    border: 2px solid var(--accent-dark);
    padding: 0.7rem 1rem;
    color: var(--accent-dark);
    transform: rotate(-1deg);
  }

  .verification-stamp span {
    font-size: 0.62rem;
    letter-spacing: 0.1em;
  }

  .verification-stamp strong {
    font-size: 0.9rem;
  }

  .verification-note {
    max-width: 56rem;
    margin-bottom: 3rem;
    border-left: 3px solid var(--accent);
    padding-left: 1rem;
    color: var(--ink-soft);
  }

  .screenshot-gallery {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin-bottom: 5rem;
  }

  .shot {
    margin: 0;
    border: 1px solid var(--ink);
    background: var(--white);
  }

  .shot-span {
    grid-column: 1 / -1;
  }

  .shot img {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    background: #0d211c;
  }

  .shot figcaption {
    padding: 0.7rem 1rem;
    border-top: 1px solid var(--line);
    font-size: 0.85rem;
    color: var(--ink-soft);
  }

  @media (max-width: 760px) {
    .screenshot-gallery {
      grid-template-columns: 1fr;
    }

    .shot-span {
      grid-column: auto;
    }
  }

  .evidence-media {
    display: grid;
    grid-template-columns: minmax(0, 1.5fr) minmax(15rem, 0.5fr);
    margin: 0 0 5rem;
    border: 1px solid var(--ink);
    background: var(--ink);
    color: var(--white);
  }

  .evidence-media video,
  .evidence-media img {
    width: 100%;
    height: 100%;
    min-height: 22rem;
    object-fit: cover;
    background: #0d211c;
  }

  .evidence-media figcaption {
    display: grid;
    align-content: space-between;
    gap: 2rem;
    border-left: 1px solid rgba(247,246,241,.3);
    padding: 1.3rem;
  }

  details {
    font-size: 0.85rem;
  }

  summary {
    color: #df9a51;
    cursor: pointer;
    font-family: var(--mono);
  }

  details ol {
    padding-left: 1.2rem;
  }

  .source-links,
  .commit-ledger {
    margin-bottom: 5rem;
  }

  .source-links > h3,
  .commit-ledger > h3 {
    margin-bottom: 1.5rem;
    font-size: clamp(1.7rem, 3vw, 2.7rem);
  }

  .source-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    border-top: 1px solid var(--ink);
  }

  .source-grid a {
    position: relative;
    display: grid;
    min-height: 10rem;
    gap: 0.5rem;
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: 1rem 3rem 1rem 1rem;
    text-decoration: none;
  }

  .source-grid a:nth-child(2n) {
    border-right: 0;
  }

  .source-grid a:hover {
    background: color-mix(in srgb, var(--accent) 9%, transparent);
  }

  .source-grid span {
    color: var(--accent-dark);
    font-size: 0.65rem;
  }

  .source-grid small {
    color: var(--ink-soft);
  }

  .source-grid b {
    position: absolute;
    top: 1rem;
    right: 1rem;
    color: var(--accent-dark);
  }

  .commit-table {
    border-top: 1px solid var(--ink);
  }

  .commit-row {
    display: grid;
    grid-template-columns: 7rem 6rem minmax(0, 1fr) 6rem;
    gap: 1rem;
    align-items: start;
    border-bottom: 1px solid var(--line);
    padding: 1rem 0;
  }

  .commit-row time,
  .commit-row code,
  .commit-row a,
  .private-mark {
    font: 0.72rem var(--mono);
  }

  .commit-row code,
  .commit-row a {
    color: var(--accent-dark);
  }

  .commit-row div {
    display: grid;
  }

  .commit-row small {
    color: var(--ink-soft);
  }

  .private-mark {
    color: var(--ink-soft);
  }

  .code-evidence {
    display: grid;
    gap: 4rem;
  }

  .code-case {
    border: 1px solid var(--ink);
    background: #102a23;
    color: #e9e7df;
  }

  .code-case > header {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.2rem;
  }

  .code-case > header span {
    color: #df9a51;
    font-size: 0.66rem;
    text-transform: uppercase;
  }

  .code-case h3 {
    margin: 0.35rem 0 0;
    font-size: clamp(1.25rem, 2vw, 1.8rem);
  }

  .code-case > header a {
    color: #df9a51;
    font: 0.72rem var(--mono);
  }

  .file-bar {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    border-block: 1px solid rgba(233,231,223,.22);
    padding: 0.65rem 1.2rem;
    color: rgba(233,231,223,.65);
    font: 0.68rem var(--mono);
    overflow-wrap: anywhere;
  }

  pre {
    margin: 0;
    overflow-x: auto;
    padding: clamp(1rem, 3vw, 2rem);
    background: #0b1f1a;
    font: 0.78rem/1.7 var(--mono);
    tab-size: 2;
  }

  .code-case > p {
    max-width: none;
    margin: 0;
    border-top: 1px solid rgba(233,231,223,.22);
    padding: 1.2rem;
    color: rgba(233,231,223,.75);
    font-size: 0.9rem;
  }

  @media (max-width: 760px) {
    .dossier-header,
    .code-case > header {
      align-items: flex-start;
      flex-direction: column;
    }

    .evidence-media,
    .source-grid {
      grid-template-columns: 1fr;
    }

    .evidence-media figcaption {
      border-top: 1px solid rgba(247,246,241,.3);
      border-left: 0;
    }

    .evidence-media video,
    .evidence-media img {
      min-height: auto;
    }

    .source-grid a,
    .source-grid a:nth-child(2n) {
      border-right: 0;
    }

    .commit-row {
      grid-template-columns: 1fr auto;
    }

    .commit-row div {
      grid-column: 1 / -1;
      grid-row: 2;
    }
  }
</style>
