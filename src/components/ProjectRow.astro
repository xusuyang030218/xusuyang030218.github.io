---
import type { Project } from '../data/projects';

interface Props {
  project: Project;
  index: number;
}

const { project, index } = Astro.props;
---

<article class={`project-row tone-${project.tone} reveal`}>
  <a class="project-link" href={`/projects/${project.slug}/`} aria-label={`查看项目：${project.title}`}>
    <div class="project-index mono">{String(index + 1).padStart(2, '0')}</div>
    <div class="project-main">
      <div class="project-meta mono">
        <span>{project.categoryLabel}</span>
        <span>{project.period}</span>
      </div>
      <h3>{project.title}</h3>
      <p>{project.summary}</p>
      <div class="tags" aria-label="技术栈">
        {project.stack.slice(0, 5).map((item) => <span class="tag">{item}</span>)}
      </div>
    </div>
    <div class="project-proof">
      {project.highlights.slice(0, 3).map((item) => <span>{item}</span>)}
    </div>
    <span class="project-arrow" aria-hidden="true">↗</span>
  </a>
</article>

<style>
  .project-row {
    border: var(--border);
    background: var(--surface);
    box-shadow: var(--shadow);
    margin-bottom: 1.25rem;
    transition: transform 90ms ease, box-shadow 90ms ease;
  }

  .project-row:hover {
    transform: translate(-2px, -2px);
    box-shadow: 8px 8px 0 var(--ink);
  }

  .project-link {
    display: grid;
    grid-template-columns: 3.5rem minmax(0, 1fr) minmax(12rem, 0.45fr) 2rem;
    gap: clamp(1rem, 3vw, 2.5rem);
    align-items: start;
    padding: clamp(1.75rem, 4vw, 3rem);
    text-decoration: none;
  }

  .project-index {
    color: var(--ink);
    font-size: 0.8rem;
    font-weight: 800;
  }

  .project-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem 1.2rem;
    color: var(--ink-soft);
    font-size: 0.72rem;
    font-weight: 700;
  }

  h3 {
    margin: 0.45rem 0 0.8rem;
  }

  p {
    margin-bottom: 1rem;
    color: var(--ink-soft);
    font-weight: 600;
  }

  .project-proof {
    display: grid;
    gap: 0.7rem;
    padding-top: 1.45rem;
    color: var(--ink);
    font-size: 0.86rem;
    font-weight: 700;
  }

  .project-proof span {
    border-left: 5px solid var(--blue);
    padding-left: 0.7rem;
  }

  .project-arrow {
    font-size: 1.4rem;
    font-weight: 900;
  }

  @media (max-width: 800px) {
    .project-link {
      grid-template-columns: 2rem minmax(0, 1fr) 1.5rem;
    }

    .project-proof {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .project-link {
      grid-template-columns: 1fr 1.5rem;
    }

    .project-index {
      display: none;
    }
  }
</style>
